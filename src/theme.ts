import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    mode: 'light',
    background: {
      paper: '#fafbfc'
    },
    primary: {
      main: '#CB3C2E',
      light: '#000000'
    },
    secondary: {
      main: '#89939a'
    }
  }
});