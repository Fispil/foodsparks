import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    mode: 'light',
    background: {
      paper: '#fafbfc'
    },
    primary: {
      main: '#313237',
      light: '#fafafa'
    },
    secondary: {
      main: '#89939a'
    }
  }
});